~~看到这的时候觉得jb太简单了，怎么还打星，没想到啊，兄弟们，一个经典的Cache 结构图，就给我看傻了~~

# 为什么

略

# 结构
~~上图~~
![[assets/Word_20260611_213646(1).pdf]]

结构一定要和流程一起看

## 缓存寻址

进程访存va -------mmu/tlb----->pa------cpu--->cache，
cache 先把 pa 化成三段，分别是Tag（标志）,index(set,组号),offset(缓存行内偏移)
然后 根据index (组号 set) 找到对应的列(怎么说，就是面向你的那四个行都是 一组，所以，图中展示的是4路组相联)，再通过tag 也就是看tag 区域是不是你要找的，tag 是用来标识的，找到相应的way(也叫路，就是单个的行，这个行 也叫做 缓存行 ，是CPU核心 和 Cache ,Cache 和 内存之间数据交换的基本单元，他的大小就是粒度，缓存行 和 页表项 类似 不仅有 想要的数据，还有一些标志位，属性位，比如二者都有的有效位), 然后 就检查缓存行 中的有效位，一般1 是有效的，如果是1 就命中了，然后再根据offset 来确定 数据在哪，最后 取得数据
![[assets/faf40010e2abb1299caf7271a7433ed8_720.jpg]]

## Cache 什么时候 和 内存 进行数据交换
![[assets/66ea7e1e6d115414195acf1eb3f1c311.jpeg]]
第一个 是机器指令

# 应用

前面提到过CPU 和 缓存之间的数据交换是 以 缓存行为粒度的，那么，不同进程之间对缓存的同时访问也是建立在访问不同缓存行的前提下的，这也是 缓存冲突的避免。至于对物理内存页的分配策略的影响就是体现在 将相邻的地址相邻的物理页分开到不同的组里面，就有了**缓存着色**的概念。
优点: 尽可能地多利用缓存空间，多存相邻的物理页，多命中，然后还支持多进程或者单进程中对相邻空间的访问。